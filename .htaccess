ErrorDocument 404 /404.html
RewriteEngine On

# ホスト名マッチはそのまま
RewriteCond %{HTTP_HOST} ^(www\.)?tack2427\.f5\.si$ [NC]
# REQUEST_URI を使って確実にパスを引き継ぐ（テストは 302）
RewriteRule ^ https://tacksanbot.onrender.com/archive%{REQUEST_URI} [R=302,L]
